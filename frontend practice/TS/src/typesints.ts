let drink = "chai"
let cups = Math.random() > 0.5 ? 10:5;
// drink = 2 error : infrencing 
// First value was string so variable will ony accept string next time ;

let name : string; 
name = "Suryansh"
// Defining the type of data is type annotation